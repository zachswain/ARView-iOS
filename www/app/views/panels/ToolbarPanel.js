app.views.ToolbarPanel = Ext.extend(Ext.Panel, {
	dockedItems : [
	    {
	    	dock : "bottom",
			id : "toolbar",
			xtype : "toolbar",
			cls : "toolbarPanel",
			scroll: "horizontal",
			
			items : [
			]
	    }
]
});